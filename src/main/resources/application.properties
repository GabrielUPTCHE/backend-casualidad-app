spring.application.name=casualidad-backend
spring.profiles.active=local
# Clave secreta para firmar los JWT (DEBE estar en Base64 y ser larga por seguridad)
# Esta clave es "mi-secreto-super-seguro-casualidad-app-2026-backend" en Base64
jwt.secret=bWktc2VjcmV0by1zdXBlci1zZWd1cm8tY2FzdWFsaWRhZC1hcHAtMjAyNi1iYWNrZW5k

# (Opcional) Tiempo de expiración del Access Token (1 día en milisegundos)
jwt.expiration=86400000

# (Opcional) Tiempo de expiración del Refresh Token (7 días en milisegundos)
jwt.refresh-token.expiration=604800000

# Esto le dice a Hibernate que actualice la BD creando las tablas que falten
spring.jpa.hibernate.ddl-auto=update

# Opcional pero recomendado para ver el SQL que se ejecuta por debajo
spring.jpa.show-sql=true

# --- Configuración de Correo (Forzando SMTPS para saltar Antivirus) ---
spring.mail.protocol=smtps
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=465
spring.mail.username=aa056b001@smtp-brevo.com
spring.mail.password=xsmtpsib-b2f59e23990f63e5417a4d8f2ab674bae55ba7666ddc7cff29ceae81326f16e6-VIAwD4Y7BWbFHJ4V

# Autenticación requerida
spring.mail.properties.mail.smtps.auth=true

# Confiar en cualquier certificado (el del Antivirus)
spring.mail.properties.mail.smtps.ssl.trust=*

# APAGAR la validación de identidad (Esto es lo que evita el error del DNS/SAN)
spring.mail.properties.mail.smtps.ssl.checkserveridentity=false

# Tiempos de espera
spring.mail.properties.mail.smtps.connectiontimeout=5000
spring.mail.properties.mail.smtps.timeout=5000
spring.mail.properties.mail.smtps.writetimeout=5000