# application-local.yml
spring:
  datasource:
    url: jdbc:postgresql://casualidad-dev-db.csjmmc6gm1oa.us-east-1.rds.amazonaws.com/casualidad_dev_db
    username: postgres
    password: casualidad-db-2026
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  datadog:
    metrics:
      export:
        enabled: false

logging:
  level:
    root: INFO
    com.tuempresa: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG